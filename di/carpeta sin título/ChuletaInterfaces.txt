
DISEÑO INTERFACES : 

public class VehiculosJFrame extends javax.swing.JFrame {

// Ó

JFrame miFrame = new JFrame ("titulo de miFrame"){

//CREATING OBJECTS
	JButton miBoton = new JButton("Saludar");
	grupoCombustible = new javax.swing.JButtonGroup();

//ADDING
	miFrame.add(miBoton);
	miFrame.add(miBoton2);
	.add(ButtonReset)
	.add(ButtonEnviar))

//ADDING TO GROUP	
	JButtonGroup1.add(jCheckBox1);
	JButtonGroup1.add(jCheckBox1);
	
//ACTIONS
	JCheckBoxMotoActionPerformed {
		if (JCheckBoxMoto.isSelected()) {
            JCheckBoxTurismo.setSelected(false);
        }	
	}
	
	

}

FUNCIONES DE PROPIEDAD: 

.setSelected(bool);
.setEnabled(bool);
.setVisible(bool);
.setText("String");
.setForeground(Color.red);
.getText();
if (jRadioButton.isSelected())    
JComboColor.getSelectedItem()
String text = JOptionPane.showInputDialog();
JOptionPane.showMessageDialog("Cuidao pescao");
    
    
    
    
  